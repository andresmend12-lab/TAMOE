{
  "rules": {
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "auth != null && auth.uid === $uid"
      }
    },
    "clients": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
